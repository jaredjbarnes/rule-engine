RULE updatePerson {
    WHEN (person.firstName == "John" && person.lastName == "Smith") || person.firstName == "Jane"
    THEN 
        person.lastName = "Doe"
        person.firstName = "Aydri" + "Jared"
        person.age = 10
}